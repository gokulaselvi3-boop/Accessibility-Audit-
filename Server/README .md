# Server

The server provides backend functionality for the Accessibility Audit project.

## Purpose

- Provide API endpoints
- Handle server-side requests
- Support accessibility audit features

## Main Endpoint

`GET /api/health`

Returns the current server status.

## Run

From the project root:

```bash
npm run dev

Then **Commit changes**.

Type **Next** when finished.
